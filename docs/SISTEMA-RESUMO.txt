===========================================================
  GAME LAUNCHER - RESUMO DE MUDANÇAS
  Última atualização: 30/06/2026
===========================================================

MUDANÇAS NESTA SESSÃO:
----------------------

1. RESPONSIVIDADE - AJUSTES COMPLETOS:

   a) Sidebar (Sidebar.tsx):
      - Menu colapsável com botão hamburger
      - Auto-colapsa abaixo de 768px (mobile)
      - Overlay escuro + sidebar fixa no mobile
      - Botão X para fechar
      - Estado collapsed com useState

   b) Library (Library.tsx):
      - Header empilha verticalmente em mobile
      - Search ocupa largura total no mobile
      - Grid responsivo: 2 → 6 colunas
      - Spacing e fontes escalonados (sm:, md:)

   c) GameCard (GameCard.tsx):
      - Hover overlay FORA da área de imagem
      - Overflow visível no card (overflow-visible)
      - Botões menores em mobile (text-[10px] sm:text-xs)
      - Badge de plataforma responsivo
      - Correção: botões não são mais cortados

   d) GameDetail (GameDetail.tsx):
      - Hero banner responsivo: h-56 sm:h-64 md:h-80
      - Capa oculta em sm:hidden
      - Título responsivo: text-xl sm:text-2xl md:text-4xl
      - Grid sidebar empilha em mobile
      - Screenshots thumbnails responsivos
      - Todos os elementos com sm:/md: prefixes

   e) TitleBar (TitleBar.tsx):
      - Altura: h-8 sm:h-9
      - Botões: w-9 sm:w-11
      - Título: text-[10px] sm:text-xs

   f) StellarBladeAchievements (StellarBladeAchievements.tsx):
      - Todos os elementos responsivos
      - Padding e gaps escalonados

   g) AddGame (AddGame.tsx):
      - Formulário responsivo
      - Inputs e botões escalonados

2. GAMECARD - CORREÇÃO DE BOTÕES CORTADOS:
   - Problema: Botões (Jogar, Detalhes, Apagar) ficavam cortados
   - Solução: Movidos para fora da área de imagem, dentro do card
   - Resultado: Botões aparecem completamente em qualquer tamanho

3. NOTIFICAÇÕES DE CONQUISTAS (ESTILO STEAM):
   - Novo componente: AchievementNotification.tsx
   - Popup no canto inferior direito com slide-in animation
   - Barra de progresso que encolhe (5 segundos)
   - Auto-dismiss após 5 segundos
   - localStorage para rastrear conquistas já vistas
   - Integração com GameDetail: detecta novas conquistas ao carregar
   - Fila de notificações para múltiplas conquistas

3. DOCUMENTAÇÃO ATUALIZADA:
   - SISTEMA-COMPLETO.md: Adicionada seção 7.1 Design Responsivo
   - SISTEMA-RESUMO.txt: Este arquivo
   - AGENTS.md: Adicionada seção Responsive Design

ARQUIVOS ATUAIS:
----------------

src/electron/
  main.ts           → Processo principal
  preload.ts        → Context bridge
  database.ts       → SQLite CRUD
  gameScanner.ts    → Scanner multi-plataforma
  stellarBladeParser.ts → Parser save Stellar Blade

src/ui/
  App.tsx           → Roteamento principal
  components/
    TitleBar.tsx    → Barra de título (responsiva)
    Sidebar.tsx     → Menu lateral (colapsável)
    GameCard.tsx    → Card de jogo (hover overlay corrigido)
    StellarBladeAchievements.tsx → Conquistas do save
  pages/
    Library.tsx     → Biblioteca (grid responsivo)
    GameDetail.tsx  → Detalhe do jogo (responsivo)
    AddGame.tsx     → Adicionar jogo (responsivo)

src/types/
  index.ts          → Interfaces TypeScript

BREAKPOINTS TAILWIND:
---------------------
sm: 640px   → Celulares grandes
md: 768px   → Tablets
lg: 1024px  → Desktops pequenos
xl: 1280px  → Desktops
2xl: 1536px → Telas grandes

IPC HANDLERS ATUAIS:
--------------------
get-games, save-games, delete-game
scan-games, select-exe, select-image
launch-game (com play time tracking)
get-cached-achievements (lê do DB)
get-play-sessions, get-stats
get-steam-store-info (screenshots, description, etc.)
has-stellar-blade-save, parse-stellar-blade-save
window-minimize, window-maximize, window-close

COMANDOS:
---------
npm run dev           → Dev mode (auto-build electron)
npm run build         → Build completo (vite + electron)
npm run electron:build → Build + pacote NSIS (.exe)
